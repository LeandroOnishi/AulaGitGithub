git init
git status
git add .

git config --global user.name "xxx"
git config --global user.email "xxx"

git commit -m "descrição de versão"
git remote add origin "https do repositorio"
git push -u origin main
git pull
git clone endereço do repositório.git


ls
..
clear
cd